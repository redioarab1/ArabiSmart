# 📱 ArabiSmart - تطبيق أخبار ذكي بالعربية

![Version](https://img.shields.io/badge/version-1.1.0-blue.svg)
![Platform](https://img.shields.io/badge/platform-Android-green.svg)
![License](https://img.shields.io/badge/license-MIT-orange.svg)

تطبيق إخباري ذكي يستخدم الذكاء الاصطناعي GPT-5.2 لترجمة وتلخيص الأخبار من مصادر متعددة باللغة العربية.

## 📋 نظرة عامة

**ArabiSmart** هو تطبيق موبايل متطور يجمع الأخبار من أكثر من 20 مصدراً موثوقاً ويستخدم تقنيات الذكاء الاصطناعي لترجمتها وتلخيصها بشكل تلقائي. التطبيق مصمم خصيصاً للمستخدمين العرب ويدعم اللغة العربية بشكل كامل مع دعم RTL.

## ✨ المميزات الرئيسية

### 🤖 الذكاء الاصطناعي
- **ترجمة تلقائية**: ترجمة الأخبار من السويدية والإنجليزية إلى العربية باستخدام GPT-5.2
- **تلخيص ذكي**: تلخيص المقالات الطويلة إلى نقاط رئيسية
- **تصنيف تلقائي**: تصنيف الأخبار حسب الفئات المختلفة

### 📰 إدارة الأخبار
- **20+ مصدر موثوق**: جلب الأخبار من مصادر عالمية ومحلية
- **الأخبار العاجلة**: قسم مخصص للأخبار العاجلة والمستجدات
- **تصنيفات متعددة**: سياسة، اقتصاد، رياضة، تكنولوجيا، صحة، ثقافة، وأكثر
- **بحث متقدم**: البحث في جميع الأخبار بسهولة

### 👤 إدارة المستخدمين
- **مصادقة آمنة**: نظام تسجيل دخول آمن باستخدام JWT
- **نظام المفضلة**: حفظ الأخبار المفضلة للقراءة لاحقاً
- **ملف شخصي**: إدارة الحساب والإعدادات الشخصية

### 🎨 واجهة المستخدم
- **تصميم عصري**: واجهة مستخدم جذابة وسهلة الاستخدام
- **الوضع الليلي**: دعم الوضع الليلي والفاتح
- **دعم RTL كامل**: دعم كامل للغة العربية من اليمين إلى اليسار
- **تجربة سلسة**: تحميل سريع وتجربة مستخدم ممتازة

## 🏗️ البنية التقنية

### Frontend (تطبيق الموبايل)
- **React Native**: إطار عمل لبناء تطبيقات الموبايل
- **Expo**: منصة لتطوير ونشر تطبيقات React Native
- **TypeScript**: لغة برمجة مع دعم الأنواع
- **Zustand**: إدارة الحالة
- **Axios**: للتواصل مع الـ Backend
- **Expo Router**: للتنقل بين الشاشات

### Backend (الخادم)
- **FastAPI**: إطار عمل Python سريع وحديث
- **MongoDB**: قاعدة بيانات NoSQL
- **JWT**: للمصادقة والتفويض
- **Python 3.11+**: لغة البرمجة

### قاعدة البيانات
- **MongoDB Atlas**: قاعدة بيانات سحابية
- **Collections**: users, articles, favorites
- **Indexes**: لتحسين الأداء

## 📦 التثبيت والإعداد

### المتطلبات الأساسية
- Node.js 18+ و pnpm
- Python 3.11+
- حساب MongoDB Atlas
- حساب Expo (للبناء)

### 1. استنساخ المستودع

```bash
git clone https://github.com/redioarab1/ArabiSmart.git
cd ArabiSmart
```

### 2. إعداد Backend

```bash
cd backend
pip install -r requirements.txt
```

قم بتحديث ملف `.env` بمعلومات قاعدة البيانات:

```env
MONGO_URL="mongodb://your-mongodb-connection-string"
DB_NAME="sample_mflix"
JWT_SECRET_KEY="your-secret-key"
EMERGENT_LLM_KEY="your-llm-api-key"
```

تشغيل الخادم:

```bash
uvicorn server:app --host 0.0.0.0 --port 8001
```

### 3. إعداد Frontend

```bash
cd frontend
pnpm install
```

قم بتحديث ملف `.env`:

```env
EXPO_PUBLIC_BACKEND_URL=http://your-backend-url:8001
```

تشغيل التطبيق:

```bash
npx expo start
```

## 🔨 بناء APK

### الطريقة 1: استخدام EAS Build (موصى به)

```bash
cd frontend
pnpm add -g eas-cli
eas login
eas build --platform android --profile preview
```

### الطريقة 2: البناء المحلي

```bash
cd frontend
npx expo prebuild --platform android
cd android
./gradlew assembleRelease
```

ملف APK سيكون في: `android/app/build/outputs/apk/release/app-release.apk`

## 📱 التثبيت على الهاتف

### من Expo (أسهل طريقة)
1. افتح رابط البناء على Expo
2. امسح رمز QR باستخدام تطبيق Expo Go
3. أو حمل ملف APK مباشرة

### من ملف APK
1. انقل ملف APK إلى هاتفك
2. افتح الملف وانقر على "تثبيت"
3. قد تحتاج إلى السماح بتثبيت التطبيقات من مصادر غير معروفة

## 🔐 الأمان

- **JWT Authentication**: مصادقة آمنة باستخدام JSON Web Tokens
- **Password Hashing**: تشفير كلمات المرور
- **HTTPS**: اتصال آمن مع الخادم
- **Environment Variables**: حماية المفاتيح السرية

## 📊 قاعدة البيانات

### Collections

#### Users
```json
{
  "_id": "ObjectId",
  "email": "string",
  "password": "hashed_string",
  "name": "string",
  "created_at": "datetime"
}
```

#### Articles
```json
{
  "_id": "ObjectId",
  "title": "string",
  "description": "string",
  "content": "string",
  "source": "string",
  "category": "string",
  "image": "string",
  "published_date": "datetime",
  "translated": "boolean"
}
```

#### Favorites
```json
{
  "_id": "ObjectId",
  "user_id": "string",
  "article_id": "string",
  "created_at": "datetime"
}
```

## 🌐 API Endpoints

### Authentication
- `POST /api/auth/register` - تسجيل مستخدم جديد
- `POST /api/auth/login` - تسجيل الدخول

### Articles
- `GET /api/articles` - جلب جميع الأخبار
- `GET /api/articles/{id}` - جلب خبر محدد
- `GET /api/articles/category/{category}` - جلب أخبار حسب الفئة
- `GET /api/articles/search?q={query}` - البحث في الأخبار

### Favorites
- `GET /api/favorites` - جلب المفضلة
- `POST /api/favorites` - إضافة إلى المفضلة
- `DELETE /api/favorites/{id}` - حذف من المفضلة

## 🚀 النشر

### Backend
- يمكن نشر Backend على:
  - Heroku
  - Railway
  - DigitalOcean
  - AWS EC2

### Frontend
- بناء APK عبر EAS Build
- نشر على Google Play Store
- توزيع داخلي عبر Expo

## 📝 الترخيص

هذا المشروع مرخص تحت رخصة MIT - انظر ملف [LICENSE](LICENSE) للتفاصيل.

## 👥 المساهمة

نرحب بالمساهمات! يرجى:
1. Fork المستودع
2. إنشاء فرع جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push إلى الفرع (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## 📞 الدعم

للدعم والاستفسارات:
- فتح Issue على GitHub
- التواصل عبر البريد الإلكتروني

## 🔄 التحديثات القادمة

- [ ] دعم iOS
- [ ] إشعارات Push
- [ ] وضع Offline
- [ ] مشاركة الأخبار
- [ ] تعليقات المستخدمين
- [ ] المزيد من مصادر الأخبار

## 📸 لقطات الشاشة

_سيتم إضافة لقطات الشاشة قريباً_

## 🙏 شكر وتقدير

- Expo Team
- React Native Community
- FastAPI Team
- MongoDB Team

---

**تم التطوير بواسطة ArabiSmart Team**

**الإصدار**: 1.1.0  
**تاريخ البناء**: 2026-01-28
