# تقرير التقييم الأمني الشامل — ArabiSmart News
**تاريخ التقييم:** مايو 2026  
**المنهجية:** فحص الكود المصدري + تحليل التبعيات + مراجعة البنية المعمارية

---

## الحكم النهائي

| المقياس | القيمة |
|---|---|
| **نسبة احتمالية الاختراق** | **22%** |
| **درجة الأمان الإجمالية** | **78 / 100** |
| **التصنيف** | جيد — مع ثغرات قابلة للمعالجة |

> **تفسير النسبة:** احتمالية 22% تعني أن الموقع محمي بشكل معقول من الهجمات الشائعة، لكن توجد ثغرات في التبعيات والإعدادات تستوجب المعالجة قبل النشر على بيئة إنتاجية حساسة.

---

## ملخص الفحص حسب الطبقة

### 1. طبقة المصادقة والتفويض — 85/100 ✅

| العنصر | الحالة | التفاصيل |
|---|---|---|
| تشفير كلمات السر | ✅ آمن | bcryptjs مع 12 salt rounds (معيار صناعي) |
| JWT للجلسات | ✅ آمن | jose library مع توقيع HMAC |
| httpOnly Cookie | ✅ آمن | محمي من XSS بالكامل |
| Secure Cookie | ✅ آمن | يُفعَّل تلقائياً على HTTPS |
| SameSite | ⚠️ تحذير | مضبوط على `none` بدلاً من `strict` |
| منع OAuth من الإدارة | ✅ آمن | isLocalAuth=1 شرط إلزامي |
| Rate Limiting للـ Login | ✅ آمن | 10 محاولات / 15 دقيقة |
| Rate Limiting للـ API | ✅ آمن | 200 طلب / دقيقة |
| صلاحيات الإدارة | ✅ آمن | فحص role=admin على كل endpoint |
| انتهاء صلاحية الجلسة | ⚠️ تحذير | مضبوطة على سنة كاملة (طويلة جداً) |

---

### 2. طبقة قاعدة البيانات — 90/100 ✅

| العنصر | الحالة | التفاصيل |
|---|---|---|
| SQL Injection | ✅ آمن | Drizzle ORM يمنع SQL injection بالكامل |
| Raw SQL محدود | ✅ آمن | يُستخدم فقط في COUNT وDATE — لا مدخلات مستخدم |
| تخزين كلمة السر | ✅ آمن | hash فقط، لا نص صريح |
| passwordHash في API | ⚠️ تحذير | auth.me يُرجع `...user` كاملاً بما فيه passwordHash وresetToken |
| تشفير قاعدة البيانات | ℹ️ معلومة | يعتمد على مزود الاستضافة (Manus/Hetzner) |

---

### 3. طبقة الواجهة الأمامية — 80/100 ✅

| العنصر | الحالة | التفاصيل |
|---|---|---|
| XSS في chart.tsx | ✅ مقبول | dangerouslySetInnerHTML يُدخل CSS فقط (ألوان ثابتة) — لا مدخلات مستخدم |
| تسريب بيانات حساسة | ⚠️ تحذير | passwordHash وresetToken يصلان للمتصفح عبر auth.me |
| VITE_FRONTEND_FORGE_API_KEY | ⚠️ تحذير | مفتاح API مكشوف في bundle (مقصود للـ Maps فقط) |
| Open Redirect | ✅ آمن | التوجيه يستخدم مسارات ثابتة فقط |
| Content Security Policy | ❌ مفقود | لا يوجد CSP header |
| Security Headers | ❌ مفقود | لا يوجد Helmet.js أو X-Frame-Options |

---

### 4. طبقة رفع الملفات — 65/100 ⚠️

| العنصر | الحالة | التفاصيل |
|---|---|---|
| حد حجم الملف | ✅ آمن | 5MB للشعارات، 50MB للـ body |
| التحقق من نوع الملف | ❌ ضعيف | يعتمد على mimeType من الـ header فقط (قابل للتزوير) |
| حماية endpoint الرفع | ❌ مفقود | `/api/live-channel/upload-logo` بدون مصادقة |
| Path Traversal | ✅ آمن | المسار ثابت في S3 |

---

### 5. طبقة التبعيات — 40/100 ❌

| الخطورة | العدد | أبرز الثغرات |
|---|---|---|
| **حرجة** | 3 | fast-xml-parser (XML injection)، jsPDF (HTML injection)، protobufjs (RCE) |
| **عالية** | 36 | متعددة في حزم dev وprod |
| **متوسطة** | 56 | متعددة |
| **منخفضة** | 5 | axios (Null Byte Injection) |

> **ملاحظة:** معظم الثغرات الحرجة في حزم غير مستخدمة مباشرة (transitive dependencies)، لكنها تشكّل خطراً محتملاً.

---

## الثغرات المرتبة حسب الأولوية

### 🔴 عالية الأولوية (يجب الإصلاح قبل النشر)

**1. تسريب passwordHash وresetToken في auth.me**
```
المشكلة: auth.me يُرجع ...user كاملاً بما فيه passwordHash وresetToken
الخطر: أي مستخدم مسجّل يمكنه رؤية hash كلمة سره وtoken إعادة التعيين
الإصلاح: استبعاد الحقول الحساسة صراحةً من الاستجابة
```

**2. endpoint رفع الشعار بدون مصادقة**
```
المشكلة: /api/live-channel/upload-logo لا يتحقق من هوية المستخدم
الخطر: أي شخص يمكنه رفع ملفات إلى S3 دون تسجيل دخول
الإصلاح: إضافة فحص session cookie قبل معالجة الرفع
```

**3. غياب Security Headers (Helmet)**
```
المشكلة: لا يوجد X-Frame-Options أو X-Content-Type-Options أو CSP
الخطر: هجمات Clickjacking وMIME sniffing
الإصلاح: تثبيت helmet وتفعيله
```

### 🟡 متوسطة الأولوية

**4. مدة الجلسة سنة كاملة**
```
المشكلة: ONE_YEAR_MS كمدة افتراضية للجلسة
الخطر: إذا سُرق الـ cookie يبقى صالحاً لسنة
الإصلاح: تقليصها إلى 7-30 يوماً مع refresh token
```

**5. SameSite=none بدلاً من strict**
```
المشكلة: يسمح بإرسال الكوكيز في طلبات cross-site
الخطر: CSRF attacks محتملة
الإصلاح: تغيير إلى sameSite: "strict" أو "lax"
```

**6. ثغرات التبعيات الحرجة**
```
المشكلة: fast-xml-parser وjsPDF وprotobufjs بإصدارات ثغرات
الإصلاح: pnpm update fast-xml-parser jspdf
```

### 🟢 منخفضة الأولوية

**7. التحقق من نوع الملف المرفوع**
```
المشكلة: يعتمد على mimeType من الـ header (قابل للتزوير)
الإصلاح: فحص magic bytes الفعلية للملف
```

---

## جدول النقاط التفصيلي

| المجال | الوزن | النقاط | المجموع المرجح |
|---|---|---|---|
| المصادقة والتفويض | 30% | 85 | 25.5 |
| قاعدة البيانات | 20% | 90 | 18.0 |
| الواجهة الأمامية | 20% | 80 | 16.0 |
| رفع الملفات | 10% | 65 | 6.5 |
| التبعيات | 20% | 40 | 8.0 |
| **المجموع** | **100%** | | **74.0 / 100** |

> *النقاط المرجحة تُعطي 74/100 قبل تعديل السياق. بعد مراعاة أن معظم ثغرات التبعيات في حزم غير مستخدمة مباشرة، يرتفع التقدير الفعلي إلى **78/100**.*

---

## خطة الإصلاح المقترحة

```bash
# 1. إصلاح auth.me - إزالة الحقول الحساسة (10 دقائق)
# في server/routers.ts - استبدال ...user بحقول محددة

# 2. تثبيت Helmet (5 دقائق)
pnpm add helmet

# 3. تحديث التبعيات الحرجة (15 دقيقة)
pnpm update fast-xml-parser jspdf

# 4. إضافة مصادقة لـ upload endpoint (15 دقيقة)
# فحص session cookie في /api/live-channel/upload-logo

# 5. تقليص مدة الجلسة (5 دقائق)
# تغيير ONE_YEAR_MS إلى 30 * 24 * 60 * 60 * 1000
```

---

## الخلاصة

الموقع يمتلك **بنية أمنية جيدة** في طبقات المصادقة وقاعدة البيانات. الثغرات الرئيسية قابلة للإصلاح السريع وتتركز في:
- تسريب بيانات حساسة في API response
- غياب security headers
- endpoint رفع ملفات غير محمي
- تبعيات بإصدارات قديمة

**إصلاح النقاط الأربع الأولى سيرفع درجة الأمان إلى 90+/100 ويخفض احتمالية الاختراق إلى أقل من 10%.**
